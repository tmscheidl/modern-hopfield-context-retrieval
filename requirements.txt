# Core deep learning
torch>=2.0.0

# Configuration management
hydra-core>=1.3.0
omegaconf>=2.3.0

# Optional (recommended for experiments, debugging, visualization)
numpy>=1.24.0
matplotlib>=3.7.0
tqdm>=4.65.0

# Optional (useful for notebooks / development)
jupyter>=1.0.0
ipykernel>=6.25.0
